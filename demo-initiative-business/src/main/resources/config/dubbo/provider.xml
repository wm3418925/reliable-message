<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/
	http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd"
	default-lazy-init="false">
	
	<!-- application name -->
	<dubbo:application name="demo-initiative-business" />
	
	<!-- registry address, used for consumer to discover services -->
	<dubbo:registry address="${dubbo.zookeeper.address}" id="defaultRegistry" />
	
	<!-- 用dubbo协议在20880端口暴露服务 -->
	<dubbo:protocol name="dubbo" serialization="kryo" port="${dubbo.service.port}" optimizer="wangmin.demo_initiative_business.core.entity.serialization.SerializationOptimizerImpl" />
	
	<dubbo:monitor protocol="registry" />
	
	<!-- dubbo接口 -->
	<dubbo:service interface="wangmin.demo_initiative_business.core.remote.DemoInitiativeBusinessServiceInterface" ref="demoInitiativeBusinessService" registry="defaultRegistry" timeout="2000" retries="0" version="1.0.0" />
</beans>

